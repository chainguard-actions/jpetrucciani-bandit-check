name: Test basic bandit check on clean code
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - name: Run bandit on clean Python code
        id: action
        uses: .
        with:
          path: ./tests/fixtures/clean
      - name: Assert bandit completed successfully
        run: |
          echo "PASS: bandit ran without finding security issues on clean code"
